<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.popularmoviesapp.popularmovies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name="br.com.popularmoviesapp.popularmovies.gui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="br.com.popularmoviesapp.popularmovies.gui.DetailsActivity"
            android:launchMode="singleInstance" />
        <provider
            android:name=".data.PopularMoviesProvider"
            android:authorities="br.com.popularmovies"
            android:exported="false"/>
        <service
            android:name=".sync.PopularMoviesSyncJobIntentService"
            android:exported="false" />
    </application>

</manifest>